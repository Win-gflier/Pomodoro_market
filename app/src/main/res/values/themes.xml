<resources
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PomodoroLike" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/grey_very_light</item>
        <item name="colorPrimaryDark">@color/grey_very_light</item>
        <item name="colorAccent">@color/grey_very_light</item>
    </style>

    <style name="NumberPickerTheme" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/white</item>
        <item name="colorControlNormal">@color/transparent</item>
        <item name="android:textColorPrimary">@color/orange</item>
        <item name="android:background">@color/white</item>
        <item name="android:textSize">30sp</item>

    </style>
</resources>